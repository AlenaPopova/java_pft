package ru.stqa.pft.sendbox;

/**
 * Created by Алёна on 13.05.2017.
 */
public class Square {
  public double l; // l- это атрибут

  public Square(double len) { //создание функции Конструктор с параметром len
    // для заполнения атрибута нового создаваемого объекта new Square() значением ,
    // который передается в качестве параметра в конструктор
    l=len; // заполнение атрибута len значением
  }
}
